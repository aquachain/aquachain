name: Grep check

on: [push, pull_request]

jobs:
  check-env-usage:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: grepping for bad fnucs
        run: bash .github/grepcheck
